@ApplicationModule(
        displayName = "Validation",
        allowedDependencies = {"core", "ledger", "pricing", "customer"}
)
package io.libra.validation;

import org.springframework.modulith.ApplicationModule;
