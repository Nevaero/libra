@ApplicationModule(
        displayName = "Settlement",
        allowedDependencies = {"core", "trading", "ledger"}
)
package io.libra.settlement;

import org.springframework.modulith.ApplicationModule;
